import * as React from 'react';
import styled from 'styled-components';

const TopBlock = styled.div``;
export interface TopProps {}

const Top: React.FC<TopProps> = props => {
  return <TopBlock>aaa</TopBlock>;
};

export default Top;
